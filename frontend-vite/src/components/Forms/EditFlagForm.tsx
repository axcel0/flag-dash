import { useFormik } from "formik";

import { FormProps } from ".";

const EditFlagForm: React.FC<FormProps> = ({
	handleSuccess,
	handleFailed,
	handleCancel,
	data,
}) => {
	return <div>EditFlagForm</div>;
};

export default EditFlagForm;
